---
name: check
description: 快速体检全部关注标的，发现异常。用法 /check。
---

# /check — 快速体检

## 触发条件
用户输入 `/check` 时触发。每日 15:30 自动触发。

## 执行步骤

### 1. 获取最新数据
```bash
cd /Users/zhaojunjie/pyProject/cycleLaboratory && python3 scripts/fetch.py --all 2>&1
```

### 2. 跑全量分析
```bash
cd /Users/zhaojunjie/pyProject/cycleLaboratory && python3 scripts/analyze.py --all 2>&1
```

注意：脚本 stdout 最后一行是 JSON 数组，其余为终端进度输出。用 `tail -1` 提取 JSON。

### 3. 输出格式
极简表格，一行一标的：

```
名称            代码      现价     评分   PE分位  价格分位  3y回撤   RSI   均线  周期阶段
══════════════════════════════════════════════════════════════════════════════════
酒ETF          512690   0.453    54     -      0.7%  -46.8%   31.3   18   底部（早期）
牧原股份        002714  37.760    67    5.8%   13.1%  -35.7%   14.8   12   接飞刀
...
```

只标出异常：
- RSI < 20 → 🔵 极度超卖
- RSI > 75 → 🔴 极度超买
- 阶段切换到「接飞刀」→ ⚠️
- 评分单日变化 > 5 分 → 📉/📈
- 已持仓浮亏 > 10% → 💀

### 4. 总结
一句话总结当前市场状态。如果全部正常，就说"今日无异常"。
如果有个别标的异常，简要说明并建议是否要深度分析。
