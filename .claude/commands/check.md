---
name: check
description: 快速体检全部关注标的，发现异常。用法 /check。
---

# /check — 快速体检

## 触发条件
用户输入 `/check` 时触发。每日 15:02 自动触发（周一至周五）。

## 执行步骤

### 1. 获取数据
```bash
cd /Users/zhaojunjie/pyProject/cycleLaboratory && python3 scripts/fetch.py --all 2>&1
```
缓存策略：收盘后 15:02 定时任务会强制拉取一次。手动 `/check` 走缓存——交易时段自动过期、收盘后自动复用，不会重复拉。

### 2. 跑全量分析
```bash
cd /Users/zhaojunjie/pyProject/cycleLaboratory && python3 scripts/analyze.py --all 2>&1
```

注意：脚本 stdout 最后一行是 JSON 数组，其余为终端进度输出。用 `tail -1` 提取 JSON。

### 3. 加载标的专属领域知识

**这是关键步骤**：`analyze.py` 只输出定量数据，不包含行业周期逻辑。必须对每个标的读取对应的 target skill 文件，融合领域知识后给出判断。

对 watchlist 中的每个标的，读取 `.claude/skills/target-{名称}-{代码}.md`：

| 标的 | skill 文件 | 核心领域知识 |
|------|-----------|-------------|
| 酒ETF 512690 | `target-酒ETF-512690.md` | 白酒 3-4 年周期、茅台批价、渠道库存、社零数据 |
| 牧原股份 002714 | `target-牧原股份-002714.md` | 猪周期 W 型双底、能繁母猪、猪粮比、头均利润 |
| 紫金矿业 601899 | `target-紫金矿业-601899.md` | 铜金资源周期、COMEX 价格、全球 PMI、TC/RC |
| 沪深300ETF 510300 | `target-沪深300ETF-510300.md` | 宏观周期、PMI、社融、M2、国债收益率 |

读取后，将领域知识应用于步骤 4 的分析：
- 结合行业周期位置判断当前处于周期的哪个阶段（而非仅看价格分位）
- 对照关键先行指标验证脚本输出的 `cycle_phase` 是否合理
- 给出符合该标的特性的操作建议和反方观点

### 4. 输出格式
极简表格，异常标记必须打在行内，行动列必须给出具体操作（不能只写"观望"）：

```
名称           代码      现价     PB分位     RSI      均线      周期阶段          持仓   行动
════════════════════════════════════════════════════════════════════════════════════════════
酒ETF         512690   0.453   🔵0.4%     28.0     空头排列   底部（早期）       -     建观察仓
牧原股份       002714  37.760   17.2%     🔵15.6    空头排列   底部（早期）       -     等信号
紫金矿业       601899  30.710   🔴91.1%    30.9     空头排列   🔴顶部             -     不参与
沪深300ETF     510300   4.932   🔴82.9%    53.9     交叉      🔴顶部             -     不追高
```

表格规则：
- **异常标记打在具体数值/字段上**，不在行首名称上打，每行可出现多个标记
- **行动列必须给出具体操作**：建观察仓 / 等信号 / 不参与 / 可加仓 / 减仓 / 清仓 等，禁止笼统写"观望""持有"
- 每个标的下方附一句话操作建议 + 反方观点

持仓列（来自 ledger.yaml 假设交易记录）：
- 「-」— 无 AI 推荐持仓
- 「⚪ 观察仓 0.4万(+2.1%)」— 已推荐建观察仓，投入金额，当前浮盈/浮亏
- 「● 1/3仓 3.3万(-5.2%)」— 已推荐第一批建仓
- 「● 2/3仓 6.7万(+1.3%)」— 已推荐第二批加仓
- 「⚫ 满仓 10万(-8.3%)」— 已推满仓
- 「已清仓」— 已推荐全部卖出

### 5. 异常标记（打在具体数据字段上）

- RSI < 20 → RSI 数值前加 🔵，如 `🔵15.6`
- RSI > 75 → RSI 数值前加 🔴，如 `🔴78.2`
- PB分位 < 5% → PB分位前加 🔵，如 `🔵0.4%`
- PB分位 > 80% → PB分位前加 🔴，如 `🔴91.1%`
- 底部（确认）→ 周期阶段后加 ✅，如 `底部（确认）✅`
- 接飞刀 → 周期阶段前加 ⚠️，如 `⚠️接飞刀`
- 顶部 → 周期阶段前加 🔴，如 `🔴顶部`
- 假设持仓浮亏 > 10% → 持仓列后加 💀

### 6. 给出建议并记录

如果建议买入/卖出操作，**直接更新 ledger.yaml** 记录假设交易：

```bash
# 买入记录格式（手动编辑 ledger.yaml 或脚本追加均可）
# 在 trades 列表末尾追加：
- date: "2026-05-28"
  symbol: "002714"
  action: buy
  type: "观察仓"
  quantity: 100
  price: 37.76
  amount: 3776
  reason: "底部早期，建观察仓"
```

用户确认采纳后，直接写入 ledger.yaml 并告知用户已记录。

### 7. 总结
一句话概括 + 各标的行动建议 + 当前假设持仓总览。

## 输出规则（重要）

**必须将完整报告输出到用户可见的对话中，禁止只在思考过程里生成。**

报告必须包含以下全部内容，缺一不可：

### A. 汇总表格
按步骤 4 格式输出。

### B. 每个标的的详细分析（逐个展开）

每个标的必须包含以下结构，**不能省略**：

```
## {名称} ({代码}) — {现价}元 · {周期阶段}

**技术面**：
| 指标 | 当前值 | 含义 |
|------|--------|------|
| RSI(14) | {值} | <30 超卖，<20 极端超卖 |
| 均线排列 | {空头/多头/交叉} | 趋势方向 |
| 布林带 | {下轨下方/下轨附近/中轨偏下/中轨偏上/上轨附近/上轨上方} | 下轨下方=极端低位，上轨上方=极端高位 |
| MACD 日线 | {金叉/死叉/中性} | 短期动量 |
| MACD 周线 | {金叉/死叉/中性} | 中期动量 |
| 成交量 | {放量/正常/萎缩} | 动能确认 |
| 换手率 | {极度低迷/偏低/正常/活跃/极度活跃} | 极度低迷=交投清淡，可能筑底 |
| 底背离 | {有/无} | 价格新低但 RSI 未新低 |

**入场点参考**（仅底部/复苏阶段标的）：
| 批次 | 触发条件 | 当前指标值 |
|------|---------|-----------|
| 观察仓 | {估值条件} | {当前估值分位} |
| 第一批 1/3 | 底背离出现 **或** 成交量萎缩至20日均量70%以下 | 底背离：{有/无} / 量比：{正常/萎缩} |
| 第二批 1/3 | 收盘站稳 MA20 **且** MA20 拐头向上 | MA20：{值} / 现价偏离：{百分比} |
| 第三批 1/3 | 回踩 MA20/MA60 不破前低 | MA60：{值} / 现价偏离：{百分比} |
| 止损 | 跌破前低 5% 以上 | 前低：{值} |

> **建议**：{1 句话操作建议}
> 
> **反方观点**：{1-2 句话，给出相反观点或风险}
```

### C. 总结

| 标的 | 一句话 | 操作 |
|------|--------|------|
| ... | ... | ... |

**当前假设持仓**：{ledger.yaml 中的持仓总览}

### 输出自查清单

输出前确认：
- [ ] 汇总表格已显示
- [ ] 每个标的的详细分析已显示（含周期位置、技术面、建议、反方观点）
- [ ] 总结表格已显示
- [ ] 所有内容在用户可见区，不在思考过程里